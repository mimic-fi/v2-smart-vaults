{
  "name": "@mimic-fi/v2-smart-vaults",
  "private": true,
  "version": "0.0.1",
  "license": "GPL-3.0",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test",
    "test:goerli": "yarn workspaces run test:goerli",
    "test:mainnet": "yarn workspaces run test:mainnet"
  },
  "dependencies": {},
  "devDependencies": {},
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
